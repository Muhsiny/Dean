{
  "name": "beheshti-academy",
  "version": "3.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node --check public/curriculum-v3.js && node --check public/app-v3.js && node build.js",
    "audit:prod": "npm audit --omit=dev --audit-level=moderate"
  },
  "engines": {"node": ">=20"},
  "devDependencies": {
    "@neondatabase/neon-js": "0.7.0-beta",
    "esbuild": "0.28.2"
  }
}
